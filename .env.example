# Copy to .env.local (or let `npx convex dev` create it for you).
# Online multiplayer, leaderboard and groups read this. Without it, the app
# still runs — those features show a "set up Convex" notice and same-screen
# play works fine.
VITE_CONVEX_URL=https://your-deployment.convex.cloud
